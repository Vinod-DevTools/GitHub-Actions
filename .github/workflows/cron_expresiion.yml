name: Trigger cron expresiion
on:
  schedule:
    - cron: "*/5 * * * *"
jobs:
  Trigger-cron-expresiion:
    runs-on: uduntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: run hello world
        run: echo Hello world
      
